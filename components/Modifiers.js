import React, {useState} from 'react'
import { Text, View } from 'react-native'

const Modifiers = () => {
  return (
    <View>
      <Text>Modifiers!</Text>
    </View>
  )
}

export default Modifiers;