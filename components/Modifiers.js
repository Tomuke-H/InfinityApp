import React, {useState} from 'react'
import { Text, View } from 'react-native'

const Modifiers = ({player1, player2, setPlayer1, setPlayer2}) => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default Modifiers;